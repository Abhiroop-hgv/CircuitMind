# CircuitMind — Handoff

**Purpose of this file:** paste it into a fresh LLM session (or a new Claude
Code instance) to restore full context without re-deriving anything. Every
number below was queried live, not recalled.

**Written:** 2026-09-10. **Repo:** `Udyoga-Pramoda-Hackathon-2026/team-satyatma`
(private). **Local path:** `C:\Users\DELL\OneDrive\Desktop\supply-chain-intel`.

---

## 1. What this project is

CircuitMind is an AI decision layer over an existing ERP for semiconductor and
PCBA manufacturers. It reads a **published** external event (export control,
port closure, fab incident), resolves it against the buyer's own BOM,
inventory, demand and supplier data, verifies substitute parts with
deterministic rules, and produces a costed multi-supplier procurement plan
that sits at `PENDING_APPROVAL` until a named person signs it. It is **not**
an ERP — no order entry, no invoicing, read-only against the system of
record.

Built for **Yukti Manthan 2.0**, an AI hackathon, by Team Satyatma (Abhiroop,
Shruthi, Aravind, Sreephaneesh, Vishnu Tej).

The one-line pitch: **"The model chooses which question to ask. The database
answers it."**

---

## 2. Tech stack (verified against requirements.txt / package.json)

| Layer | Technology |
|---|---|
| Database | PostgreSQL 17, two schemas, raw SQL via psycopg 3 (`psycopg[binary,pool]`) |
| API | FastAPI, uvicorn, server-sent events for long-running pipelines |
| Language model | Groq, `openai/gpt-oss-120b`, JSON-schema constrained decoding |
| Forecasting | statsmodels — Holt-Winters exponential smoothing |
| BOM parsing | pdfplumber (tables), pypdf, openpyxl |
| Frontend | Next.js 16 (App Router), TypeScript, plain CSS, oklch tokens |
| Tests | pytest, 55 tests, all passing |
| Deployment (intended) | Render, both halves |

Note: `requirements.txt` also lists `anthropic>=0.40` under "Intelligence &
LLM Extractors" — this is **vestigial**. The actual model in use everywhere
is Groq's `openai/gpt-oss-120b` (`agents/intelligence/groq_extract.py`,
`agents/assistant/agent.py`). Worth removing or confirming dead before
anyone assumes Claude is called anywhere in the pipeline.

---

## 3. Architecture

```
                    news notice                 a new board's BOM
                         │                              │
                         ▼                              ▼
            ┌────────────────────────┐      ┌────────────────────────┐
            │  1 intelligence agent  │      │      BOM intake        │
            │  constrained extract   │      │  parse → resolve →     │
            │  → match → score       │      │  register → buildable? │
            └───────────┬────────────┘      └───────────┬────────────┘
                        └──────────────┬────────────────┘
                                       ▼
                        ┌──────────────────────────┐
                        │  3 supply risk agent     │◀── 2 demand forecast
                        │  running stock ledger    │    (Holt-Winters)
                        └────────────┬─────────────┘
                                     ▼
                        ┌──────────────────────────┐
                        │  4 component agent       │
                        │  retrieve → verify       │  ← deterministic rules
                        └────────────┬─────────────┘
                                     ▼
                        ┌──────────────────────────┐
                        │  5 procurement agent     │
                        │  exclude → adjust → plan │
                        └────────────┬─────────────┘
                                     ▼
                            ╔═══════════════════╗
                            ║  a person signs   ║
                            ╚═════════╤═════════╝
                                      ▼
                            purchase orders, one per supplier
```

Plus a sixth surface not in that chain: **Ask**, a tool-calling assistant
(`agents/assistant/`) with 10 read-only tools over the same tables.

### Two-schema database boundary

- **`erp`** — read-only mirror of the system of record: `products`, `bom`,
  `components`, `suppliers`, `supplier_components`, `inventory`,
  `warehouses`, `customer_orders`, `purchase_orders`,
  `purchase_order_items`, `production_plans`. Never written by the app.
- **`platform`** — everything the analysis produces: `external_events`,
  `event_impacts`, `demand_forecasts`, `shortages`, `alternatives`,
  `supplier_scores`, `purchase_recommendations`,
  `purchase_recommendation_lines`, `build_requests`, `build_request_lines`,
  `v_stock_position` (view).

### The invariants (do not violate these in any change)

1. The model is never the source of a factual number — every figure traces
   to a database query.
2. Only events that have **already happened** are analysed. Nothing is
   forecast except ordinary demand (Holt-Winters), which is a different
   thing from predicting geopolitics.
3. Compatibility is decided by a deterministic rule engine
   (`agents/component/rules.py`), never by the model. Similar part numbers
   are not treated as compatible.
4. Unknown BOM parts are ranked for a human, never auto-matched.
5. Nothing is ordered without a named human approval. No code path
   transmits a purchase order.
6. The assistant connects as `scip_readonly` — two independent locks
   (privilege grants + `default_transaction_read_only`), so the database
   itself refuses writes even if the tool layer were wrong.

### Enforcement of invariant 1, concretely

| Mechanism | What it does |
|---|---|
| Constrained decoding | Event category is a JSON-schema `enum` built from the live catalogue — the model cannot emit a class the DB doesn't have |
| Read-only role | `scip_readonly`; verified — INSERT/UPDATE/DELETE/CREATE all refused |
| Output guard (`agents/assistant/guard.py`) | BLOCKs an answer with figures but no tool call behind it; FLAGs a figure absent from any tool result |

### Repo layout

```
agents/{intelligence,demand,supply_risk,component,procurement,assistant,bom_intake}/
api/{main,config,serialization,streaming,pipeline}.py
api/routers/{health,catalogue,recommendations,assistant,runs,bom}.py
db/{connection,schema.sql,seed_master.sql,seed_orders.py,migrations/}
integration/circuitmind/       drop-in compatibility_score for a related repo
scripts/                       demo, seeding, reset, sample-BOM generators
tests/                         conftest, test_rules, test_parser, test_guard_and_intake
web/src/app/                   / /ask /bom /events /login /recommendations
                                /recommendations/[id] /runs /shortages /suppliers
artifacts/                     submission decks, scripts, architecture diagrams
```

### HTTP surface

```
GET  /api/health · /api/overview · /api/events · /api/impacts
GET  /api/shortages · /api/suppliers
GET  /api/recommendations · /api/recommendations/{id}
GET  /api/recommendations/{id}/po · /po.zip · /po/manifest
POST /api/recommendations/{id}/approve
POST /api/ask · /api/bom/preview
POST /api/run/news · /api/run/bom          (SSE-streamed)
```

### Key constants

- `TODAY` pinned to `2026-09-03` in `api/config.py` — the seeded data
  describes one specific week
- `WATCH_MPN = "STM32F407VGT6"`
- `RISK_PREMIUM = 0.15`, `DUAL_SOURCE_PREMIUM_LIMIT = 0.05`,
  `MAX_LINES = 3` (procurement agent)
- RNG seed for the generated dataset is fixed (`db/seed_orders.py`), so
  `init_db.py` reproduces identical numbers on every run

---

## 4. Canonical demo scenario (recommendation #1)

Live, verified numbers — this is what the pitch decks quote:

> An export licence notice hits **STM32F407VGT6**, used on the **MC-3000**
> and **SD-220** boards. Coverage leaves **5,130 units short by 15
> October**. **STM32F429VGT6** clears the rule engine as a substitute; two
> look-alikes are rejected on footprint — **LQFP144** and **LQFP64** against
> the board's **LQFP100** land pattern (a similarity score would have
> recommended both). Procurement splits the buy three ways, weighted by
> on-time record:

| Supplier | Units | Unit price | Line total | Arrives | On-time |
|---|---|---|---|---|---|
| Avnet Asia Pte Ltd | 3,000 | $11.05 | $33,150.00 | 15 Sep | 95% |
| DigiKey Electronics | 2,000 | $11.20 | $22,400.00 | 10 Sep | 99% |
| Mouser Electronics | 130 | $10.85 | $1,410.50 | 22 Sep | 98% |

**Total: 5,130 units, $56,960.50, landed 23 days early.**
Status: `PENDING_APPROVAL`.

### Second scenario (recommendation #3) — built this session

A BOM-driven story, deliberately different in shape: the Sensor Hub SH-100
board (already registered via `scripts/bom_scenario.py`) reassessed at a
higher build quantity. **MX25L12835FM2I-10G flash, short 7,000 units.** No
substitution needed — the original part passes the rule engine — but **no
single distributor holds 7,000** (largest is Avnet at 4,429), so procurement
splits it three ways anyway: Nexperia 720 / Fujian Longtai 1,851 / Avnet
4,429, total **$12,892.13**. Status: `PENDING_APPROVAL`.

This pairs with #1 to show the same engine handling two different problem
shapes: "we need a different part" and "we need the same part, from more
than one place." Every number is real agent output — nothing fabricated;
build quantity is the only chosen input, same as the original demo script
hardcodes `BUILD_QTY = 500`.

Recommendation **#2** (500 ceramic capacitors, $4.70, `APPROVED`) is a
stray from earlier `bom_scenario.py` testing, superseded by #3. Left in the
database — not deleted without being asked — but it clutters the
Recommendations list for a demo. **Candidate cleanup, not yet done.**

---

## 5. What changed this session

### Code added

| File | Purpose |
|---|---|
| `scripts/make_mixed_bom_xlsx.py` | Generates a 40-line BOM that's ~70% resolvable against the catalogue, 30% genuine unknowns (real passives/connectors/mechanical parts), plus 2 DNP lines at qty 0. Verified through the actual parser, not just by construction. |
| `scripts/make_bom_split_po.py` | Drives the second demo scenario: reassesses the Sensor Hub board at a chosen build quantity through the real `BuildabilityAgent` → `ComponentAgent` → `ProcurementAgent` chain, producing recommendation #3. |
| `samples/MC-3000_RevD_BOM.xlsx` | Output of the script above; a realistic mixed-resolution BOM fixture. |
| `artifacts/Technical-Architecture.md` | Engineering-level architecture doc, framing the whole system as retrieval-augmented (structured RAG — the corpus is the schema, not a vector store) rather than only the assistant. |
| `artifacts/Dark-Friday-Pitch-Script.md` | A 2:15 role-play script (four team members surface the coordination problem, no product shown) for the pitch's cold open. |
| `MASTER_PROMPT.md` (repo root, untracked) | Full context-transfer document for briefing another LLM on the product — invariants, data model, agents, worked example, known limitations. |
| `artifacts/mock-purchase-orders/` | Pre-generated split-PO PDFs for both scenarios (#1 and #3), as a live-demo fallback if generation glitches on the day. |

### Presentation decks edited (Canva, via MCP)

Two decks, both committed:

- **`Research and Discovery` deck** — fixed a broken repo link (wrong path
  form + a typo), named the product (was "Satyatma" only), corrected
  "5–10 professionals targeted" to the one real interview conducted
  (T Prabhanjan Kumar, Schneider Electric), promoted two shipped features
  (External Risk Intelligence, AI Assistant) from COULD to MUST, repaired a
  broken competitive-analysis table row, added the BOM-intake problem as a
  named row on the problem slide, fixed a stray full-bold formatting
  regression, straightened a persona card with leftover indentation, added
  a missing citation.
- **`Ideation & Planning` deck** — added three new slides (Problem
  Statement, Existing Solutions, Our Solution) covering all three built
  capabilities (event resolution, BOM intake, the assistant) instead of
  only the event story; replaced a text-ladder architecture slide's content
  (it had credited "Decision Engine (similarity search...)" — exactly
  backwards, since similarity explicitly does **not** decide compatibility
  in this system) with the real pipeline; laid out a user-supplied flow
  graphic in place of a navy filler panel.

### Bugs found and fixed

1. **Decks claimed a stack that doesn't exist.** "Node.js + TypeScript +
   Express" and "Vercel" were in the original Final Presentation deck. Real
   stack is FastAPI (Python) on Render for both halves. Corrected on the
   architecture slide.
2. **The vendor-split arithmetic didn't sum.** A deck claimed
   "3,000 / 2,000 / 1,000" (= 6,000) against a 5,130-unit requirement. Real
   split is 3,000 / 2,000 / **130**, pulled from the live recommendation.
3. **"Predicts shortages" contradicted the product's own no-prediction
   invariant.** Reworded everywhere to "reads a published event and
   resolves it," which is both true and the more defensible claim.
4. **Similarity was credited where rules actually decide.** The
   architecture slide said an AI "Decision Engine" did similarity search +
   vendor scoring + PO split. Corrected: AI proposes candidates: a
   deterministic rule engine decides fit.
5. **A whole database vanished between sessions.** The original Postgres
   container (Docker, port 5433) was gone on a later session with no
   trace of Docker itself being installed — cause never conclusively
   identified (see Outstanding Issues). Rebuilt a fresh, self-contained
   PostgreSQL 17 cluster via `initdb` (not Docker) so the demo no longer
   depends on Docker at all. Every figure in section 4 above reproduced
   **identically** after the rebuild, because the seed generator uses a
   fixed RNG seed.
6. **A demo-data experiment leaked into the live dashboard.** Running the
   second-scenario script at a high build quantity caused
   `BuildabilityAgent` to reassess the *entire* board, not just the
   featured part, writing 21 stray shortage rows (up to 28,180 units on an
   unrelated inductor) and inflating "Active shortages" from 2 to 23 with
   a meaningless part as the dashboard headline. Cleaned up: deleted the
   21 stray rows, kept the one that backs recommendation #3, reset the
   build request's `build_qty` back to its documented value (500).
7. **`.claude/` (Claude Code's own tooling config) was committed to the
   repo.** Removed from git tracking and added to `.gitignore`; kept
   locally so the dev-server launch config still works.
8. Multiple deck-level factual corrections logged under "Bugs found" in the
   Canva edit history above (link, arithmetic, prediction language,
   similarity claim) — listed there rather than repeated here.

---

## 6. Outstanding issues

**Ordered roughly by how much they'd cost you in front of a jury.**

1. **The repo is private.** `github.com/Udyoga-Pramoda-Hackathon-2026/team-satyatma`
   404s for anyone outside the org. It's a named artifact on slide 1 of two
   decks. Needs to go public, or judges added as members, before
   submission. Not verified this session (no `gh` CLI available locally).
2. **The Render deployment's database is stale.** It was showing its one
   recommendation as `APPROVED` (dashboard reads "Awaiting approval: 0")
   last checked. All the rebuild/reset work this session was against the
   **local** database only. If judges click the live Render link, they'll
   see the wrong state. Needs `scripts/reset_approval.py` run against
   Render's `DATABASE_URL`, which lives in Render's dashboard, not in the
   local `.env`.
3. **Two abandoned PostgreSQL data directories, undiagnosed.**
   `C:\Users\DELL\.scip-pgdata` (dotted, hidden — last touched 6 Sept, and
   the path `README.md` and `scripts/pg_start.ps1` *currently* document)
   versus `C:\Users\DELL\scip-pgdata` (no dot — created 10 Sept, what's
   actually running right now). The docs point at the wrong path. Nobody
   has looked inside the dotted directory to see whether it holds older,
   possibly more complete demo state, or whether it's simply an earlier,
   abandoned attempt at the same recovery this session did again
   independently. **Needs a decision:** either fix the docs to point at
   the live path, or migrate onto the documented one — not both left
   dangling.
4. **This Postgres cluster is not a Windows service.** It does not survive
   a reboot. `scripts/pg_start.ps1` exists to bring it back up (points at
   the wrong — dotted — path per issue 3; will need its path corrected
   regardless of which directory wins). **Must be run after every
   restart, before anything else, or the whole app is dead in the water.**
5. **Recommendation #2 (the stray $4.70 capacitor) is still `APPROVED`**
   in the database, cluttering the Recommendations screen next to the two
   real demo scenarios. Not deleted without being asked. A one-line
   `DELETE` if you want it gone.
6. **The dashboard picks the wrong headline shortage.** With two
   recommendations pending, the overview banner surfaced the BOM-flash
   story ahead of the STM32 one — looks like sorted by need-by date or
   insertion order rather than severity/value. If the STM32 story should
   always lead, this needs an actual code look (likely the `ORDER BY` in
   the overview query in `api/routers/catalogue.py` or wherever
   `/api/overview` is served) — not diagnosed further this session.
7. **`requirements.txt` lists `anthropic>=0.40` but nothing in the codebase
   calls Anthropic's API** — the model everywhere is Groq. Either dead
   weight or a sign something was migrated away from Claude without
   updating the dependency list. Worth a `grep -r anthropic agents/ api/`
   to confirm before removing.
8. **Slide 6 of the Ideation deck still shows wireframes branded
   "SupplyChain Sentinel"** — an old product name — rather than real
   screenshots of the running app. Flagged repeatedly, not yet replaced.
9. **1 local commit ahead of `origin/main`, unpushed** as of this writing.
10. **No CLAUDE.md exists yet.** A `/init` was requested and interrupted
    mid-investigation (the pgdata-path discovery above is what triggered
    the pause) before the file was written. Still to do.

---

## 7. How to run it (as currently configured — see issue 3/4 above)

```bash
# 1. bring the database up (does NOT survive reboot — do this first, always)
& "C:\Program Files\PostgreSQL\17\bin\pg_ctl.exe" `
    -D "C:\Users\DELL\scip-pgdata" `
    -l "C:\Users\DELL\scip-pgdata\server.log" `
    -o "-p 5433" start

# 2. confirm .env points at the live cluster
#    DATABASE_URL=postgresql://scip@127.0.0.1:5433/scip
#    DATABASE_URL_RO=postgresql://scip_readonly:<pw>@127.0.0.1:5433/scip
#    GROQ_API_KEY=<key>

# 3. api
python -m uvicorn api.main:app --host 127.0.0.1 --port 8000

# 4. dashboard
cd web && npm run dev          # http://localhost:3000

# 5. sign in with any name; role doesn't gate anything yet
```

Rebuilding from scratch (schema + seed) if the cluster is ever lost again:

```bash
python scripts/init_db.py              # schema, master data, migrations, seeded transactions
python scripts/create_readonly_role.py # scip_readonly role for the assistant
python scripts/demo.py                 # regenerates recommendation #1 end to end
python -m pytest                       # 55 tests, should all pass
```

The seed generator's RNG is pinned — this reproduces every figure in
section 4 identically, verified this session after exactly this kind of
rebuild.

---

## 8. Explicit next steps

In priority order for demo readiness:

1. Fix the Render deployment's database state (issue 2).
2. Make the repo public or add judges (issue 1).
3. Resolve the two-pgdata-directory situation and correct
   `README.md` / `scripts/pg_start.ps1` to match reality (issue 3).
4. Write `CLAUDE.md` (the `/init` that got interrupted).
5. Decide on recommendation #2 cleanup (issue 5) — cosmetic, quick.
6. Replace slide 6's wireframes with real screenshots (issue 8).
7. Push the one local commit; commit the untracked files listed in
   section 5 that are meant to ship (decks are already committed;
   `MASTER_PROMPT.md`, the two new scripts, and the mock-PO artifacts are
   currently untracked — confirm which of these belong in the repo versus
   staying local working files before committing).
8. If time permits: investigate the overview-banner ordering (issue 6) and
   the `anthropic` dependency (issue 7).
